# When true, only expose the smile annotation flow (login + annotate).
# All other routes redirect to /smile-login.
# Set at build time: VITE_HIDE_DEV_TABS=true npm run build
# VITE_HIDE_DEV_TABS=true
